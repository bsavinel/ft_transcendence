# to create a new nest project :
# the '.' is when the project directory is already created and we are in
# (if not just give a name and it will create the dir)
# the git option is to tell nest to NOT create and init a git repo
# the '--strict' option will start the project with TypeScript compiler flags enabled: 
# strictNullChecks, noImplicitAny, strictBindCallApply, forceConsistentCasingInFileNames, 
# noFallthroughCasesInSwitch

npx nest new . --skip-git


# add typeOrm as a dependency: (pg is the driver for postgresql database)

npm install --save @nestjs/typeorm typeorm pg

# Configuring the database connection information with TypeOrm importing it
# in NestJS:
# create a typeorm config file and set the conncetion config datas.
# import the typeorm config in nestjs in the app.module file.
